[project]
name = "comfyui-prompt-sync"
version = "1.0.0"
description = "把桌面上的「提示词工具」和 ComfyUI 接起来：点按钮直接改节点里的提示词"
license = { text = "MIT" }
requires-python = ">=3.9"
dependencies = []

[project.urls]
Repository = "https://example.invalid/comfyui-prompt-sync"

[tool.comfy]
PublisherId = "local-prompt-tool"
DisplayName = "提示词同步（来自提示词工具）"
Icon = ""
